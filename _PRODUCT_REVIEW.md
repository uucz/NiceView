# 产品复盘：Nice View v0.3.0 后用户视角缺口分析

日期：2026-05-14  
范围：`uucz/NiceView` v0.3.0 fork，包含 iOS / AltStore 发布链、iOS 系统相册保存、方向/分类/标签发现、标签预览与收藏。
结论：v0.3.0 已经从“能安装、能随机浏览”推进到“能保存、能筛选、能收藏”的可用版本。下一阶段最重要的不是继续扩张入口，而是让用户更容易发现已有能力、避免被服务端限流误伤、把 11 万级标签与 6 万级图集转化为可持续探索体验，并给本地缓存与偏好一个可控出口。

## 当前已有能力

1. 安装与发布
   - GitHub Actions 生成 Android APK、未签名 iOS IPA 和 AltStore `source.json`。
   - AltStore 源地址为 `https://uucz.github.io/NiceView/source.json`。
   - v0.3.0 已通过 iOS 实机安装与更新验证。

2. 随机浏览
   - 支持 `GET /v1/random` 与 `GET /v1/random/meta`。
   - 支持标签、方向、分类、排除标签等筛选组合。
   - 预加载队列按筛选条件隔离，切换筛选后不会继续展示旧筛选队列。
   - 支持双指缩放、双击/按钮还原、下一张、网络失败重试。

3. 标签与分类发现
   - 接入 `/v1/featured-tags`、`/v1/tags`、`/v1/categories`。
   - 侧栏展示精选标签、热门标签、分类和方向筛选。
   - 支持标签预览，预览后可直接使用该标签随机。
   - 当前标签可持久化保存，重启后仍恢复。

4. 保存与本地留存
   - iOS 通过 MethodChannel 写入系统 Photos，用户已确认主页与浏览记录保存均进入系统 Photos。
   - Android 继续走平台相册保存。
   - 浏览历史保留最近 30 张。
   - 收藏独立缓存到 `Application Support/favorites`，不会被历史上限淘汰，用户已确认重启后仍保留。

5. 元数据展示
   - 当前图片展示 Image ID、Gallery ID、方向、尺寸、分类、图集标题、Content-Type、获取时间。
   - 当前图片标签可点选为筛选条件。

## 用户旅程拆解

### 第一次打开

用户目标：
- 快速看到第一张图。
- 知道下一张、保存、收藏在哪里。
- 知道还有筛选、历史、收藏这些能力。

当前状态：
- 主界面沉浸式，第一张图与下一张按钮清晰。
- 保存和收藏按钮可见。
- 侧栏主要依赖左滑打开，缺少显式菜单按钮；历史、筛选、标签发现和额度信息都藏在侧栏里。

缺口：
- 新用户不一定知道左滑可以打开侧栏。
- 长按删除标签、长按删除历史图仍是隐性操作。
- 没有首次轻引导或空状态提示，用户可能把 App 理解为只有“下一张、保存、收藏”。

### 找自己想看的内容

用户目标：
- 不想猜标签名。
- 能从热门、分类、方向、标签预览中逐步收窄。
- 遇到不喜欢的标签或类型时能长期避开。

当前状态：
- 已有精选标签、热门标签、分类、方向、排除标签。
- 标签搜索只是对已加载的少量标签做本地过滤。
- 排除标签和方向/分类筛选不作为长期偏好保存。

缺口：
- 站点标签规模约 11.7 万，App 只加载前 24 个热门标签和少量精选标签，覆盖范围很窄。
- 用户输入一个不在本地列表里的标签时，没有“全量标签搜索 / 标签页 / 搜索结果为空但可手动尝试”的清晰路径。
- 排除 `AI Generated` 等内容偏好需要每次重新设置，长期使用成本高。

### 从一张喜欢的图继续探索

用户目标：
- 看到喜欢的当前图后，继续看同标签、同图集、同模特/系列。
- 查看图集里相邻图片或更多同图集内容。
- 快速从当前标签做“使用 / 排除 / 预览”。

当前状态：
- 当前图片展示图集标题和标签。
- 标签可点选为筛选条件。
- App 尚未接入 `/v1/galleries` 与 `/v1/gallery/{id}`。

缺口：
- 图集 ID 和标题只是静态信息，没有“进入图集”“继续同图集”。
- 当前图片标签只能直接使用，不能从同一位置排除或预览。
- 站点图集详情返回图片列表、分页、标签、分类、模型名、来源页和下载链接；App 没有把其中适合移动端的“图集图片序列”和“图集标签”转化为浏览体验。
- 外部下载链接和来源页不一定适合在 App 内直接暴露，后续图集页应先聚焦站内图片浏览。

### 保存、收藏和以后再找

用户目标：
- 保存到系统相册后能在系统 Photos 找到。
- 喜欢的图不会因为继续浏览而消失。
- 能理解“历史”和“收藏”的区别。
- 能管理本地占用空间。

当前状态：
- iOS 系统 Photos 保存已实机通过。
- 收藏独立缓存并可重启保留。
- 历史和收藏在历史页用分段筛选区分。

缺口：
- 没有缓存大小、清理缓存、调整历史上限、清空历史、批量管理收藏。
- 收藏删除有二次确认，但历史/收藏的存储占用对用户不可见。
- 系统相册保存成功后，App 内缓存仍会保留；长期使用可能积累空间压力。

### 网络差、请求多或被限流

用户目标：
- 清楚知道是网络问题、服务端限流、图片不存在还是本地缓存丢失。
- 不因为快速浏览或反复标签预览被封禁。
- 在网络差时仍能看历史和收藏。

当前状态：
- App 有 70 次 / 300 秒的本地请求额度，低于站点公告的 100 次 / 300 秒随机接口封禁线。
- 服务端 429 会进入锁定提示。
- 历史和收藏可本地打开。

缺口：
- `/v1/image/{id}` 站点策略是 60 次 / 300 秒；当前图集预览和元数据筛选会间接调用固定图片接口，本地额度模型没有独立计算这个桶。
- 标签预览一次会加载 6 张固定图片，重复预览容易接近 `/v1/image/{id}` 限制，但额度条不会准确反映。
- 发现数据加载失败只通过 Snackbar 提示，侧栏内没有明确重试和降级内容。
- 没有“离线模式”语义，网络不可用时用户只能从历史页间接获得价值。

### 可访问性与平台感

用户目标：
- VoiceOver 用户能理解每个按钮。
- 字体放大后布局不乱。
- iOS 操作有自然触感反馈。
- App 图标、启动页、权限描述可信。

当前状态：
- 大多数按钮有 tooltip 或文本。
- UI 视觉风格统一，核心按钮较清晰。

缺口：
- 自定义小图标按钮和图片舞台缺少系统化 `Semantics` 标签。
- 动态字体和超长标签在侧栏中已有截断，但还没有专项验证。
- iOS haptic feedback、reduce motion、App icon 和 launch screen 仍可提升平台完成度。

## 优先级判断

### P0.5：可发现性与额度可靠性

这是下一阶段最应该先做的组合。原因：
- v0.3.0 的能力已经存在，但入口藏得较深，用户可能无法自然使用。
- 快速浏览与标签预览越好用，请求越容易变多；如果额度模型不准，用户会把 429 或封禁理解为 App 不稳定。

建议：
- 主界面增加显式侧栏/筛选按钮，位置与收藏按钮形成稳定导航关系。
- 首次启动只提示一次：筛选入口、下一张、保存、收藏、历史。
- 历史页和标签区给删除提供显式菜单或选择模式，不只依赖长按。
- 将本地额度拆为随机/元数据桶与固定图片桶，标签预览和图集图片加载也进入本地节流。
- 标签预览加短期缓存，避免同一标签反复拉 6 张固定图片。

### P1：完整标签浏览与长期偏好

建议：
- 增加独立“标签浏览”页，支持搜索、排序、分页加载、预览和使用。
- 不直接一次性拉取 11 万标签；优先使用分页加载和本地缓存最近搜索结果。
- 保存长期偏好：默认方向、默认分类、常驻排除标签。
- 当前图片标签点击后弹出动作：使用标签、预览标签、排除此标签。

### P1：图集延续探索

建议：
- 接入 `/v1/galleries` 和 `/v1/gallery/{id}`。
- 从当前图片进入图集页，展示图集封面信息、图片序列、标签和分类。
- 支持“继续同图集下一张 / 上一张”。
- 初期不暴露外部来源页和外部下载链接，避免把移动端体验引向不可控站点。

### P1：数据管理与设置

建议：
- 增加设置页：缓存占用、清理临时缓存、清空历史、调整历史上限、清理未收藏缓存。
- 收藏可批量取消或批量保存到系统相册，但批量系统保存要有明确确认。
- 展示“系统相册保存”和“App 内缓存”是两个概念，避免用户误解。

### P2：离线与降级体验

建议：
- 网络失败时，主界面提供“查看历史 / 查看收藏”入口。
- 侧栏发现数据失败时保留已缓存标签，并给局部重试按钮。
- 当前图片缺失时，若有 image_id 可以尝试恢复；恢复失败再提示移除。

### P2：可访问性与平台 polish

建议：
- 给主操作、侧栏小按钮、预览网格、历史网格补充 `Semantics`。
- 用大字体和小屏幕验证侧栏、标签 chip、历史页。
- iOS 收藏、保存、下一张、切换筛选加入轻触反馈。
- 补 iOS app icon、launch screen 与更明确的权限说明文案。

## 明确不做

- 反馈 / 举报入口：用户已明确本阶段不要做。
- 系统分享：用户已明确不要做。
- 立即向上游提 PR：当前先继续 fork 内开发，等下一批功能完成后再评估是否拆分贡献。
- 直接把站点外部下载链接做成移动端下载器：它会把产品边界从“看图客户端”拉到外部下载管理，风险和维护成本都明显提高。

## 下一步推荐

第一批先做“可发现性 + 额度可靠性”。它们不依赖新大功能，却能显著提高 v0.3.0 已有能力的实际可用性，并减少用户在高频浏览时遇到不清楚原因的失败。

第二批做“完整标签浏览 + 长期偏好”。这会把站点 11 万级标签资源真正转化为 App 的发现能力。

第三批做“图集延续探索”。这是从随机浏览走向长期使用的关键：用户喜欢一张图后，需要自然继续，而不是只能再赌下一张随机。
