# 产品复盘：Nice View 用户视角缺口分析

日期：2026-05-14  
范围：`uucz/NiceView` v0.2.1 fork，包含 Android 原能力与新增 iOS / AltStore 发布链。  
结论：当前已经完成“能安装、能浏览、能切标签、能保存、能看历史”的最小可用闭环，但距离用户稳定长期使用还有明显差距。最高优先级不是继续堆功能，而是补齐 iOS 保存语义、筛选发现能力、内容反馈入口和基础可发现性。

## 当前已有能力

1. 随机浏览图片
   - 调用 `GET /v1/random`
   - 支持按单个 `tag` 参数筛选
   - 支持下一张、预加载、限流保护和 429 服务端锁定提示

2. 本地状态
   - 记住最后一张当前图片
   - 保存最近 30 张浏览历史
   - 保存预加载队列，避免重启后立即空白
   - 本地持久化标签和当前选中标签

3. 图片查看
   - 支持双指缩放、双击还原
   - 支持历史图片全屏预览和左右切换
   - 支持缺失历史文件清理

4. 标签
   - 有默认标签：`原神`、`樱岛麻衣`
   - 用户可手动添加、删除标签
   - 标签删除不删除历史图片

5. 下载 / 保存
   - Android 通过 MediaStore 保存到系统图片库
   - 非 Android 当前写入 App Documents

6. 发布
   - Android Release APK workflow
   - iOS 未签名 IPA workflow
   - AltStore `source.json` 自动生成
   - GitHub Release 与 GitHub Pages 发布
   - v0.2.1 已通过 iOS 设备 AltStore 实机安装验证

## 用户真实需求与缺口

### P0：iOS 保存图片语义不一致

证据：`DownloadService.saveImage` 仅 Android 走原生 MediaStore；非 Android 写入 `getApplicationDocumentsDirectory()`。但 UI 在主页面和历史预览中都提示“已保存到系统相册”。

用户影响：
- iOS 用户点击下载后，以为图片进入相册，但实际只在 App 沙盒 Documents。
- 用户无法在系统相册、聊天软件、文件选择器里自然找到图片。
- 这是信任问题，不是普通增强项。

建议：
- iOS 增加原生 MethodChannel，调用 `UIImageWriteToSavedPhotosAlbum` 或 Photos framework。
- 在生成 iOS 工程时写入 `NSPhotoLibraryAddUsageDescription`。
- AltStore `appPermissions.privacy` 同步声明照片添加权限。
- 保存成功文案按平台返回路径或相册语义区分。

### P0：筛选能力远低于 API 能力

站点首页公开支持：
- `orientation`
- `category`
- `include_tag`
- `exclude_tag`
- `include_category`
- `exclude_category`
- `/v1/tags`
- `/v1/categories`
- `/v1/featured-tags`
- `/v1/galleries`
- `/v1/random/meta`
- `/v1/tag/{name}/preview`
- `/v1/feedback`

当前 App 只用到：
- `/v1/random`
- `/v1/random?tag=...`
- `/v1/image/{id}`

用户影响：
- 想要竖图、横图、分类、排除不喜欢内容、浏览标签云的用户没有入口。
- 当前标签需要手动输入，用户不知道有哪些标签可用。
- 随机图片 App 的核心体验应是“找到自己想看的随机性”，不是只给一个输入框。

建议：
- 第一步补 `/v1/featured-tags`、`/v1/tags`，做标签发现与搜索。
- 第二步补方向筛选：不限 / 竖图 / 横图。
- 第三步补排除标签和分类筛选。
- 后续补图集浏览和元数据详情。

### P0：内容反馈 / 举报入口缺失

站点有 `POST /v1/feedback`，并把反馈类型分为功能建议、失效链接、内容举报、Bug 等。App 没有对应入口。

用户影响：
- 遇到失效、违规、不适内容时无处反馈。
- 随机图片产品天然需要快速反馈入口，否则质量控制只能靠站点端。

建议：
- 当前图片抽屉增加“反馈 / 举报”按钮。
- 自动带上 `imageId`、`galleryId`、当前标签、App 版本、平台。
- 支持用户填写补充说明和可选联系方式。

### P1：关键功能可发现性不足

当前主要交互：
- 右下角下一张
- 左下角下载
- 左滑打开侧边栏
- 长按标签删除
- 长按历史图片删除

用户影响：
- 新用户可能不知道侧边栏存在，也不知道历史、标签、额度都在那里。
- 长按删除属于隐性操作，容易被忽略。

建议：
- 主界面增加一个轻量“菜单 / 信息”图标打开侧边栏。
- 首次启动显示一次短引导：左滑筛选、右下下一张、左下保存。
- 历史页给删除提供显式选择模式或菜单，不只依赖长按。

### P1：收藏与历史不是同一个需求

当前历史最多 30 张，超过会删除本地缓存文件。用户真实需求通常分两类：
- 历史：最近看过，可自动清理。
- 收藏：我明确想留下，不应被淘汰。

用户影响：
- 用户可能以为历史里喜欢的图会一直在，实际超过 30 张后会被清理。
- 保存到相册前，收藏缺失会造成流失。

建议：
- 增加“收藏 / 取消收藏”。
- 收藏不计入 30 张历史淘汰。
- 历史页增加全部 / 收藏筛选。

### P1：元数据没有转化成用户价值

当前抽屉展示 Image ID、Gallery ID、Content-Type、获取时间。API 的 `/v1/random/meta` 还可提供 width、height、orientation、gallery、tags。

用户影响：
- 用户看不到图片尺寸、方向、标签归属、图集标题。
- 无法从当前图片继续探索同标签、同图集。

建议：
- 获取图片流后补一次 meta，或优先使用 meta 取 ID 再加载图片。
- 抽屉展示尺寸、方向、标签、图集标题。
- 标签可点选，图集可进入详情。

### P1：分享能力缺失

用户会自然想做：
- 分享图片到聊天软件
- 复制图片 ID
- 复制固定图片 URL：`/v1/image/{id}`
- 分享当前筛选条件

建议：
- 增加系统分享入口。
- 增加复制固定链接入口。

### P2：设置与数据管理缺失

建议：
- 清理缓存
- 调整历史上限
- 预加载数量设置
- 重置标签
- 导出 / 导入标签
- 显示当前缓存占用

### P2：可访问性与平台感

建议：
- 为主操作按钮补 `Semantics` 标签。
- 支持动态字体时避免布局溢出。
- iOS 增加 Haptic feedback。
- 处理 reduce motion。
- 补 iOS app icon、launch screen、权限描述。

## 推荐路线图

### 第一批：信任与基础闭环

1. iOS 真正保存到系统相册。
2. 主界面增加显式侧边栏入口。
3. 当前图片反馈 / 举报。
4. 标签发现：精选标签 + 搜索标签。

### 第二批：筛选与留存

1. 方向筛选。
2. 排除标签 / 分类筛选。
3. 收藏。
4. 分享图片 / 复制固定链接。

### 第三批：探索体验

1. 图集详情。
2. 元数据详情。
3. 同标签 / 同图集继续浏览。
4. 历史与收藏管理。

## 是否应该给原作者提 PR

建议：最终应该提，但不要把当前 fork 原样提上去。

理由：
- 上游是 MIT 许可，且仓库仍活跃，提 PR 在协作和维护上更健康。
- 我们补的是通用能力：iOS 构建、AltStore 发布、保存体验、API 能力映射，并非只适合 fork。
- 但当前 fork 包含 `uucz.github.io/NiceView/source.json`、项目记忆文件和 fork 发布验证记录，不适合直接合并进上游。

推荐 PR 策略：
1. 先开 issue 或 discussion，说明我们已经验证 iOS / AltStore 可用，询问作者是否愿意接收 iOS 发布链。
2. 第一个 PR 只做“上游中立”的 iOS / AltStore workflow：
   - 移除 `uucz` 硬编码。
   - README 使用上游 URL 或占位说明。
   - 不包含 `_PROJECT_*` 文件。
   - 明确 Pages 需要启用 GitHub Actions 部署。
3. 第二个 PR 做 iOS 保存到相册。
4. 后续产品功能按小 PR 拆分：标签发现、方向筛选、反馈入口、收藏。

不建议：
- 不建议一次性提交当前 fork 全量改动。
- 不建议把 fork 的项目记忆、风险表、证据表提交给上游。
- 不建议在未修复 iOS 保存语义前，把 App 描述为“完整 iOS 体验”。

## 当前最重要的下一步

先修 P0：iOS 保存到系统相册。  
这是用户已经可以安装后的第一个真实体验缺口，也是后续给原作者 PR 时最能体现质量的部分。
