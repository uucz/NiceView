# Nice View v0.3.0 后续实施路线图

日期：2026-05-14  
目标：在 v0.3.0 已经完成 iOS 保存、筛选发现和收藏之后，把 App 推进到“入口清楚、请求稳定、能深入探索、能长期管理”的版本。反馈/举报和系统分享继续排除；上游 PR 暂不发起。

## 0. 证据摘要

已验证站点能力：

| 能力 | 接口或页面 | v0.3.0 App 状态 | 下一步判断 |
| --- | --- | --- | --- |
| 随机图片流 | `GET /v1/random` | 已使用 | 继续保留 |
| 固定图片流 | `GET /v1/image/{id}` | 已使用 | 需要独立额度桶 |
| 元数据随机 | `GET /v1/random/meta` | 已使用 | 继续作为复杂筛选入口 |
| 精选标签 | `GET /v1/featured-tags` | 已使用 | 可缓存并展示更新时间 |
| 标签列表 | `GET /v1/tags` | 只取前 24 个 | 需要标签浏览页与分页/缓存 |
| 全部标签页面 | `/tags` | 未使用 | 站点用 `limit=20000` + 本地搜索排序，可借鉴但不能简单照搬到移动端 |
| 分类 | `GET /v1/categories` | 已使用 | 当前仅精选分类，够用 |
| 标签预览 | `GET /v1/tag/{name}/preview` | 已使用 | 需要缓存和固定图片额度控制 |
| 图集列表 | `GET /v1/galleries` | 未使用 | 可做图集探索入口 |
| 图集详情 | `GET /v1/gallery/{id}` | 未使用 | 可做同图集继续浏览 |
| 站点配置 | `GET /v1/site-config` | 未使用 | 可用于公告、资源规模、限流策略展示 |
| 反馈接口 | `POST /v1/feedback` | 未使用 | 明确不接入 |

当前站点样本：
- `/v1/site-config` 返回约 `63763` 图集、`444108` 图片、`117187` 标签、`44` 分类，并公告随机接口 `300s / 100 次`、固定图片接口 `300s / 60 次`。
- `/v1/tags?limit=12` 返回 `total=117187`，说明 App 的 24 个热门标签只覆盖很小一部分标签空间。
- `/v1/galleries?limit=3` 返回图集分页、封面、分类、图片数量和更新时间。
- `/v1/gallery/{id}` 返回图集标签、图片序列、分页、来源页和外部下载链接。移动端初期应只使用站内图片序列与标签，不直接做外部下载器。

## 1. 阶段一：可发现性与请求稳定

### 用户目标

用户不需要猜手势，也不应该因为 App 自动预览或预加载触发服务端 429。

### 改造点

1. 主界面显式入口
   - 在主界面增加筛选/侧栏按钮。
   - 保留左滑手势，但不让手势成为唯一入口。
   - 按钮位置避免和收藏、保存、下一张冲突。

2. 首次轻引导
   - 首次启动显示一次短引导。
   - 内容只覆盖筛选入口、下一张、保存、收藏、历史。
   - 用户关闭后持久化，不反复打扰。

3. 删除操作可见化
   - 标签提供更多菜单或删除图标。
   - 历史和收藏网格提供显式选择/删除入口。
   - 长按仍保留为快捷方式。

4. 额度模型拆分
   - 保留随机/元数据桶，低于站点 `300s / 100 次`。
   - 新增固定图片桶，低于站点 `/v1/image/{id}` 的 `300s / 60 次`。
   - `imageById`、标签预览、图集图片加载都必须进入固定图片桶。
   - 服务端 429 时按实际接口桶进入冷却。

5. 标签预览缓存
   - 同一标签短期内复用预览结果和已加载缩略图。
   - 预览失败在 sheet 内显示局部重试，不只弹 Snackbar。

### 验收标准

- 新用户不用手势也能打开筛选/历史入口。
- 快速连续预览标签不会绕过本地额度控制。
- 额度条能解释随机请求和固定图片请求的剩余额度或冷却状态。
- 标签预览失败不影响当前主图浏览。

## 2. 阶段二：完整标签浏览与长期偏好

### 用户目标

用户能从大量标签中找到自己想看的内容，并把不想看的内容长期排除。

### 改造点

1. 标签浏览页
   - 从侧栏进入独立页面。
   - 支持搜索、按图集数排序、按名称排序。
   - 支持分页加载与滚动加载。
   - 支持标签预览、使用标签、加入我的标签、排除标签。

2. 标签缓存策略
   - 不一次性拉取 11 万标签。
   - 缓存最近页、最近搜索和精选标签。
   - 如果站点未来提供服务端搜索参数，再优先切到服务端搜索。

3. 长期内容偏好
   - 持久化默认方向。
   - 持久化默认分类。
   - 持久化常驻排除标签。
   - 提供“本次筛选”和“默认偏好”的清晰区分。

4. 当前图片标签动作
   - 当前图片标签点击后弹出动作。
   - 动作包括使用标签、预览标签、排除此标签、加入我的标签。

### 验收标准

- 用户可以搜索不在首屏 24 个热门标签内的标签。
- 重启后默认方向、分类和常驻排除标签仍生效。
- 当前图片里的标签能直接转化为继续探索或排除。
- 标签浏览页在弱网失败时保留已缓存数据并允许重试。

## 3. 阶段三：图集延续探索

### 用户目标

用户喜欢一张图后，可以自然继续看同图集、同系列或同标签，而不是只能继续随机。

### 改造点

1. 图集详情模型
   - 新增 `GalleryDetail`、`GalleryImageSummary`。
   - 解析图集标题、分类、标签、图片数量、分页和图片 ID。
   - 忽略或隐藏外部下载链接，初期不做外部下载入口。

2. API Client
   - 新增 `galleries({limit, offset})`。
   - 新增 `gallery(id, {limit, offset})`。
   - 图集图片加载走固定图片桶。

3. UI
   - 当前图片的图集标题可点击进入图集页。
   - 图集页展示图片序列，支持进入全屏预览。
   - 支持同图集上一张/下一张。
   - 支持使用图集标签继续随机。

### 验收标准

- 从当前图片能进入图集页。
- 图集页可以分页加载图片序列。
- 图集内图片保存和收藏复用既有能力。
- 不暴露外部下载链接，不改变产品边界。

## 4. 阶段四：设置与数据管理

### 用户目标

用户知道 App 占用了多少空间，也能主动清理历史、临时缓存和不再需要的收藏。

### 改造点

1. 设置页
   - 展示历史数量、收藏数量、临时缓存占用、收藏缓存占用。
   - 展示当前版本和 AltStore 源信息。

2. 清理能力
   - 清理临时预加载缓存。
   - 清空浏览历史。
   - 清理缺失文件记录。
   - 调整历史上限。
   - 批量取消收藏。

3. 语义说明
   - 明确系统 Photos 保存和 App 内缓存是两套存储。
   - 清理 App 缓存不会删除系统 Photos 中已保存图片。

### 验收标准

- 设置页能显示可理解的缓存占用。
- 用户可以清理历史和临时缓存，收藏不会被误删。
- 清理动作都有确认文案和结果提示。

## 5. 阶段五：离线、可访问性与平台 polish

### 用户目标

用户在网络差或使用辅助功能时仍能稳定使用 App。

### 改造点

1. 离线体验
   - 主图加载失败时提供查看历史和收藏的入口。
   - 发现数据失败时在侧栏局部重试。
   - 保留上次成功加载的标签和分类缓存。

2. 可访问性
   - 补充主操作按钮、侧栏小按钮、历史网格、预览网格的 `Semantics`。
   - 验证动态字体下侧栏和底部按钮不溢出。
   - 验证 VoiceOver 顺序。

3. iOS 平台感
   - 收藏、保存、下一张、切换筛选加入轻触反馈。
   - 补 iOS app icon 和 launch screen。
   - 权限描述继续保持 Info.plist 与 AltStore `source.json` 一致。

### 验收标准

- 断网时用户能直接进入历史或收藏。
- VoiceOver 能读出核心按钮用途。
- 动态字体下主要页面无文字重叠。
- iOS 安装权限元数据继续通过 AltStore 校验。

## 6. 发布与回归策略

开发期间继续避免频繁打 tag 和触发完整发布 CI。每批功能完成后：

1. 先跑本地可用的脚本检查与单元测试。
2. 批量完成后再统一打 tag。
3. 通过 GitHub Actions 跑 iOS AltStore Release 和 Android Release APK。
4. 核对 Release 资产、IPA size、`source.json`、AltStore 源。
5. 再进行 iOS 实机回归。

## 7. 上游 PR 暂停策略

当前仍不向上游提 PR。等下一批功能稳定后再评估。未来如需贡献，建议拆分为：

1. 上游中立的 iOS / AltStore 发布链。
2. iOS 保存到系统相册。
3. API query / models 重构。
4. 可发现性与标签发现。
5. 收藏与历史管理。

上游 PR 不应包含 fork 专属地址、`_PROJECT_*` 文件、产品复盘文档或 fork 发布记录。

## 8. 技术选型矩阵

| option | maturity | performance | security | compatibility | score | notes |
| --- | --- | --- | --- | --- | --- | --- |
| 标签页分页加载 | 高 | 高 | 高 | 高 | 9 | 移动端更稳，避免一次性加载 11 万标签 |
| 标签页一次性拉取 20000 | 中 | 中 | 高 | 中 | 6 | 站点 Web 可用，但移动端内存和弱网风险较高 |
| 固定图片独立额度桶 | 高 | 高 | 高 | 高 | 9 | 与站点 `/v1/image/{id}` 策略一致，能减少 429 |
| 继续单额度桶 | 中 | 高 | 中 | 中 | 5 | 实现简单，但无法解释预览和图集加载导致的限流 |
| 图集页仅站内图片 | 高 | 高 | 高 | 高 | 8 | 保持看图客户端边界 |
| 图集页暴露外部下载链接 | 中 | 中 | 低 | 中 | 4 | 产品边界变复杂，外部站点风险高 |

推荐：标签页采用分页加载；请求额度拆成随机/元数据桶与固定图片桶；图集页初期只做站内图片浏览。

## 9. 下一步执行建议

优先实施阶段一。它能直接提高 v0.3.0 已有功能的实际可用性，并降低高频浏览时触发限流的概率。阶段一完成后，再进入标签浏览与长期偏好。
